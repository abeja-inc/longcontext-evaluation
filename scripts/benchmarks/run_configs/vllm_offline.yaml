base_config: base.yaml

generator:
  type: vllm_offline_inference
  vllm_config:
    serve:
      extra_args:
        max_model_len: 8192
    generation:
      max_new_tokens: 256
      chat_template_kwargs:
        enable_thinking: false
