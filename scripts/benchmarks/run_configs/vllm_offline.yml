base_config: ./base.yml

model:
  root: ~/models
  name: example-model

generator:
  type: vllm_offline
  vllm_config:
    serve:
      extra_args:
        max_model_len: 8192
    generation:
      max_new_tokens: 256
      chat_template_kwargs:
        enable_thinking: false
