output_root: outputs/benchmarks
dataset_root: datasets/benchmarks
batchsize: 1

run_name: null
llm: null

wandb:
    null
    # entity: xxx
    # project: longcontext_evaluation_benchmark

benchmarks:
    openai_mrcr:
        mrcr_eng:
            2needle:
                language: english
                dataset_filename: english/2needle.parquet
                inference_mode: chat
                metric: all
                settings:
                    truncate: true
            4needle:
                language: english
                dataset_filename: english/4needle.parquet
                inference_mode: chat
                metric: all
                settings:
                    truncate: true
            8needle:
                language: english
                dataset_filename: english/8needle.parquet
                inference_mode: chat
                metric: all
                settings:
                    truncate: true
        mrcr_jpn:
            2needle:
                language: japanese
                dataset_filename: japanese/2needle.parquet
                inference_mode: chat
                metric: all
                settings:
                    truncate: true
            4needle:
                language: japanese
                dataset_filename: japanese/4needle.parquet
                inference_mode: chat
                metric: all
                settings:
                    truncate: true
            8needle:
                language: japanese
                dataset_filename: japanese/8needle.parquet
                inference_mode: chat
                metric: all
                settings:
                    truncate: true
