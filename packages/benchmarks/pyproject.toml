[project]
name = "benchmarks"
version = "0.1.0"
description = "Benchmarks"
authors = [{ name = "Tomoki Fujihara", email = "tomoki.fujihara@abejainc.com"}]

readme = "README.md"
requires-python = ">=3.11,<3.15"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
